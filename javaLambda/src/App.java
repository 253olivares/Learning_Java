public class App {
    public static void main(String[] args) throws Exception {

        // Lambda expression = feature for java 8 and above
        // known as an anonymous method
        // a shorter wat to write anonymous classes with only one
        // method
        // needs to use a functional interface or use a pre defined one
        // they contain only one abstract method

        // lambda expression can be used in any place where a

        // (arguments) -> {statement/s}

        // String name = "Miguel";
        // char symbol = '!';

        // MyInterface myInterface = (x, y) -> {
        // System.out.println("Hello World!");
        // System.out.println("It is a nice dat, no?" + x + y);
        // };

        // MyInterface myInterface2 = (x, y) -> {
        // System.out.println("Hello " + x + y);
        // };

        // myInterface.message(name, symbol);

        MyFrame myFrame = new MyFrame();

    }
}
