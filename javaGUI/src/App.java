public class App {
    public static void main(String[] args) throws Exception {

        // create a frame
        MyFrame myFrame = new MyFrame();

        // shortcut for frame if we do not wish to manipulate the class outside its
        // object
        // new MyFrame();
    }
}
